---
name: 'gametest-automate'
description: 'Automated test scenario generator. Use when the user says ""I want to create automated game tests"" or ""Generate test scenarios for Unity Unreal or Godot""'
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
1. Always LOAD the FULL @bmad.lens.release/_bmad/core/tasks/workflow.yaml
2. READ its entire contents - this is the CORE OS for EXECUTING the specific workflow-config @bmad.lens.release/_bmad/gds/workflows/gametest/automate/workflow.yaml
3. Pass the yaml path @bmad.lens.release/_bmad/gds/workflows/gametest/automate/workflow.yaml as 'workflow-config' parameter to the workflow.yaml instructions
4. Follow workflow.yaml instructions EXACTLY as written to process and follow the specific workflow config and its instructions
5. Save outputs after EACH section when generating any documents from templates
</steps>
