```prompt
---
description: Show full Lens context — state, config, branches, and extended info
---

Activate @lens agent and execute /lens:

1. Load agent: `_bmad/lens/agents/lens.agent.yaml`
2. Execute /lens command (read-only, expanded view)
3. Display full context including:
   - Active initiative details
   - Current phase and gate status for all phases
   - Full expanded checklist (all items, not just summary)
   - Branch topology (all initiative branches)
   - Recent event log entries
   - Background errors (if any)
   - Config settings
   - Available commands with descriptions

This is the expanded version of /status — use it for full visibility.

```
