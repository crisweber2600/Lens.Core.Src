```prompt
---
description: Manually override Lens state with audit logging
---

Activate @lens agent and execute /override:

1. Load agent: `_bmad/lens/agents/lens.agent.yaml`
2. Execute /override command
3. Require a reason for the override
4. Show current state.yaml for targeted edits
5. Apply user-provided changes and validate schema
6. Write updated state.yaml and append event-log entry

```
