```prompt
---
description: Display current initiative state, phase, branches, and checklist summary
---

Activate @lens agent and execute /status:

1. Load agent: `_bmad/lens/agents/lens.agent.yaml`
2. Execute /status command (read-only)
3. Read state.yaml and display compact view:
   - Active initiative name, type, and phase
   - Current branch and audience
   - Checklist summary (X/Y complete)
   - Background errors (if any)
   - Next suggested command

```
