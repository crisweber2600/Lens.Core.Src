# LENS Workbench Service Map
# Source of truth for TargetProjects repo inventory
# Generated by: @lens onboard
# Date: 2026-02-25T00:00:00Z
#
# Add entries to repos[] for each code repository managed by this BMAD control repo.
# Each repo entry maps a repository name to its remote URL and local path.
#
# Example:
#   - name: my-service
#     remote_url: https://github.com/crisweber2600/my-service
#     local_path: TargetProjects/my-domain/my-service
#     default_branch: main
#     domain: my-domain
#     service: my-domain

target_projects_path: ../TargetProjects

docs_output_path: docs

repos:
  - name: bmad.lens.src
    remote_url: https://github.com/crisweber2600/bmad.lens.src
    local_path: TargetProjects/lens/lens-work/bmad.lens.src
    default_branch: main
    domain: lens
    service: lens-work

  # ── GOVERNANCE REPO ──────────────────────────────────────────────────────────
  # Universal governance artifacts: constitutions, roster, policies.
  # Decoupled from the control repo so cross-initiative rules are never captured
  # on an initiative branch.  All agents MUST clone this before any governance
  # workflow executes.  Write path: universal/{slug} → main in this repo.
  # ─────────────────────────────────────────────────────────────────────────────
  - name: bmad.lens.governance
    remote_url: https://github.com/crisweber2600/bmad.lens.governance
    local_path: TargetProjects/lens/lens-governance
    default_branch: main
    domain: lens
    role: governance  # Special role — not a target project, never receives initiative branches
