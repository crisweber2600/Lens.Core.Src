# Create Split Feature

Use `create-split-feature` to create the new feature shell in the governance repo.

1. Confirm the new feature id and name.
2. Check `feature-index.yaml` for duplicates before writing anything.
3. Create the new feature directory, `feature.yaml`, `summary.md`, and `stories/` folder.
4. Append the new feature entry to `feature-index.yaml`.